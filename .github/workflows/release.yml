name: Build and release

on:
  push:
    tags:
      - '*'

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      profiles: '["release"]'
      create_release: true
      retention_days: 90
